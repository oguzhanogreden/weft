# Website build progress (Ralph ledger)

Each iteration completes ONE phase, commits, exits. Next iteration reads this first.
Phases reordered by true dependency. Mark `[x]` when committed + validated.

- [x] **P1 — markdown-loader**: `parseDoc` + types (`src/lib/markdown-loader.ts`); `virtual:weft-docs` plugin (`src/lib/docs-plugin.ts`); frontmatter on all docs + new `docs/api/dom.md`; 13 tests. Toolchain re-pinned to Vite+ 0.2.x.
- [x] **P2 — nav**: pure `buildNav(docs)` + wired `navGroups`/`flatNav`/`firstDocPath`/`findNav` (`src/lib/nav.ts`); 10 tests. Mocks `virtual:weft-docs` in tests via `vi.mock`.
- [x] **P3 — code-block**: `CodeBlock({ tokens, lang?, raw })` (`src/components/code-block.ts`) — `Component.gen`; pre/code chrome, lang label, Effect copy handler (clipboard → "Copied" flash via `SubscriptionRef`, non-fatal on denial); SSR-inert. 5 SSR tests via shared `src/__tests__/ssr.ts` (`renderString` + NoRpc). Styles in `app.css`. Browser copy interaction deferred to P10.
- [x] **P4 — demos registry**: `reactive-counter` + `reactive-input` (`src/demos/*.ts`, plain `(): Node => Effect.gen(...)`) + `demos`/`getDemo` (`src/demos/index.ts`); 4 tests (lookup, fresh-instance, dangling-id guard over real docs, SSR smoke). No doc references a `demo=<id>` yet — add one in P8/P10 for the e2e live-demo path. **Learning:** `Component.gen` requires a props arg even with an empty body → use plain `() => Effect.gen(...)` for zero-prop components.
- [x] **P5 — demo block**: `Demo({ id, tokens, lang?, raw })` (`src/components/demo.ts`, `Component.gen`) — preview pane (`getDemo(id)()`) over a `CodeBlock` code pane; unknown id → inline warning + code pane, no throw. 2 SSR tests. Styles in `app.css`. Hydrate interactivity → P10.
- [x] **P6 — render-hast**: pure `renderHast(HastNode) → Renderable[]` (`src/lib/render-hast.ts`); tag allowlist (disallowed → skip, keep children), prop mapping (className→class, id/href/src/alt/title/style passthrough, colSpan/rowSpan, data-\*), `pre`→`CodeBlock`, `dataDemo`→`Demo`. **Note:** Weft `style` prop accepts a string, so Shiki inline token colors pass through (added `style` to passthrough). Type-test + 10 unit tests incl real parseDoc→renderHast→SSR integration.
- [x] **P7 — docs-shell**: `DocsShell` (`src/layouts/docs-shell.ts`, `Component.gen`) — reads live path from `Router.currentMatch.changes`; reactive sidebar (active highlight), TOC (h2–h3), prev/next, around `Router.Outlet`. Exported as the **component** (P8 wraps it with `Router.layout({ component: DocsShell }, routes)` — circular dep avoided). Pure render helpers `renderSidebar`/`renderToc`/`renderPrevNext`/`TopBar` exported + 7 SSR tests; layout CSS in `app.css`. **Full reactive wiring (currentMatch/outlet) validated in P8** via a real `/docs/...` SSR render. **Note:** `aria-current` prop rejects `null` — use conditional spread `...(active ? { "aria-current": "page" } : {})`. Internal links are plain string paths (router intercepts same-origin clicks).
- [x] **P8 — routes**: `routes/docs.ts` (`/docs` alias + `/docs/:category/:slug`) + `routes/api.ts` (`/api` alias + `/api/:pkg`); shared `routes/doc-page.ts` (`DocPage` via `renderHast`, title h1 only if markdown lacks one); wired into `app.ts` under `DocsShell`; per-route `<title>`/meta in `shell.ts`. 10 SSR integration tests against the **real** app + real docs (no mock). **Doc data is a module singleton `liveDocs` (`lib/docs-live.ts` → `lib/docs-service.ts` `makeDocs`), NOT an Effect service** — see below.
- [x] **P9 — home**: landing page (`src/routes/home.ts`, `HomePage`) — hero (tagline + getting-started/GitHub CTAs), live `reactive-counter` demo, 4 differentiators, build-time-highlighted code teaser, footer. Replaces `<h1>Home</h1>`; full-width (no DocsShell). Teaser highlighted via a second virtual module `virtual:weft-home-snippet` added to `docs-plugin.ts` (rendered through `renderHast`→`CodeBlock`). 5 SSR tests. **Note:** route `component` slot needs `() => Node` (not `() => Renderable`).
- [~] **P10 — e2e + finalize**: e2e written + production verified; two toolchain gates remain (not website code).
  - ✅ `website.browser.test.ts` written (DocPage render + live-demo hydrate/click).
  - ✅ `vp check` green; ✅ all 66 website node tests pass (`vp test website`).
  - ✅ **Production build works** (`vp run -F website build`): client+server bundles; client bundle has **no** shiki/remark/unified/micromark/mdast runtime (AC8 purity verified), only baked data.
  - ✅ **Prod serve verified**: all routes 200 (`/`, `/docs`, `/docs/:c/:s`, `/api`, `/api/:pkg`), unknown → 404, `<title>` from frontmatter, hashed client `<script>` present. Fixed: SSR config was missing the `@tailwindcss/vite` plugin (`@import "tailwindcss"` failed in the server build) — added.
  - ❌ **`vp run test:browser` cannot execute** — browser stack broken: `@vitest/browser@4.1.9` fails to resolve `vitest/internal/browser`, and Chromium won't connect in this sandbox. Installed the missing `@vitest/browser-playwright@4.1.9`; the `internal/browser` resolution is a deeper hoisting/version mismatch. Affects the 16 existing example browser tests identically.
  - ❌ **`vp run test` (full repo) not green** — pre-existing: `vp test` can't resolve the `~/*` tsconfig alias (vitest 4.1.9), so `packages/*` tests fail to load (website unaffected — uses relative + `@weftui/*`).
  - **Both ❌ are repo-wide incomplete-vite-plus-0.2.x-migration gaps, not website code. No COMPLETE until they (or the user's call on them) resolve.**

## Notes

- **Router service bug (P8):** an app-wide Effect service (`Context.Tag`) is **not**
  reachable from route components — neither ambient `Effect.provide(render, L)` nor
  `provideService` on the outlet node works, because the reactive outlet's leaf stream
  drains in `renderToStringHydratable`'s top context. Filed as
  `packages/router/ambient-context-propagation.specs.md`. Workaround: doc model is a
  **module singleton** `liveDocs` imported directly by components. Test seam without
  mocking: the `weftDocs` plugin is registered in the **root** `vite.config.ts` so
  `virtual:weft-docs` resolves under `vp test` to the real baked docs.
- **Pre-existing toolchain gap:** `vp test` cannot resolve the `~/*` tsconfig path
  alias (vitest 4.1.9 / vite-plus 0.2.x), so `packages/*` tests that import `~/index`
  fail to load (27 files) — even run from their own package dir. Repo-wide, predates
  this work, unrelated to the website (website tests use relative + `@weftui/*` imports
  and all pass). The 0.2.x migration needs a tsconfig-paths fix. Revisit at P10 before
  claiming `vp run test` green.
- **Toolchain (P1):** catalog `vitest: 4.1.9` (real vitest that vite-plus 0.2.1 depends on), global `vp` upgraded to 0.2.1. `vp test`/`vp check` green. Run node tests with `vp test <name>`.
- **Testing the virtual module:** node tests can't resolve `virtual:weft-docs` (root vitest has no website plugin). Mock with `vi.mock("virtual:weft-docs", () => ({ getAllDocs: () => [] }))` and unit-test the pure builder directly (see `nav.test.ts`).
- `render-hast` (P6) consumes hast-style camelCase props: `className` array → `class`; code `<pre>` carries `dataLang`/`dataRaw`/`dataDemo`; `dataDemo` present → render `Demo` instead of `CodeBlock`. `CodeBlock` receives already-rendered `tokens: Renderable[]` (renderHast renders the `<code>` children then hands them in — no import cycle).
- Doc model facts: api section = core/dom/router (order 1/2/3); `firstDocPath` → `/docs/guides/getting-started`; `docs/index.md` excluded (no frontmatter).
- **Before writing P3/P5/P9 components:** study Weft component API — read `docs/concepts/combinator-api.md`, `docs/concepts/reactive-primitives.md`, and an example app (`examples/declarative-event-handlers`, `examples/subscription-ref`) for `h`, reactive handlers, `SubscriptionRef`, stream-as-children, SSR/hydrate-inert patterns. No JSX.
