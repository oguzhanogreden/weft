/**
 * Ambient declarations for non-code asset imports handled by Vite. `noUncheckedSideEffectImports`
 * requires a module type for side-effect imports like `import "./app.css"`.
 */
declare module "*.css" {}
