name: Setup
description: Setup pnpm, Node.js, and install dependencies

runs:
  using: composite
  steps:
    - name: VP Setup
      uses: voidzero-dev/setup-vp@v1
      with:
        node-version: 26
        cache: true
